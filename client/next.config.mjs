/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "avatars.githubusercontent.com",
      "xsgames.co",
      "files.edgestore.dev",
    ],
  },
};

export default nextConfig;
